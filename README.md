# Calculations Project

## Overview

- **CalculationsApi** is built with .NET and uses SQL Server for persistence.
- **CalculationsUI** is a React front-end application.

## Installation Instructions

1. Run `CreateTables.sql` to create the history table.
2. Start the backend API (`CalculationsApi`).
3. Start the frontend UI (`CalculationsUI`).

